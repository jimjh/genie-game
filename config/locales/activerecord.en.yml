en:
  activerecord:
    errors:
      models:
        lesson:
          attributes:
            url:
              invalid: is not a valid git URL
        answer:
          attributes:
            base:
              not_published: lesson is not published
        access_request:
          attributes:
            base:
              already_granted: request has already been granted
            requestee:
              not_found: is not a registered user
            requestee_id:
              taken: already has a pending request
